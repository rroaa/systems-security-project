# provide the additional filename that you have identified as a potentially suspicious file. Also provide details about the Command and control Domain which is hardcoded inside this file. 

A bash file with name 'SSH-One' is founded, it eliminates the firewall rules by turning them off, and it modifies the rc.local to run SSH-T & SSH-One files when the system starts. 

# Filename
SSH-One

# Callout domain
http://darkl0rd.com
